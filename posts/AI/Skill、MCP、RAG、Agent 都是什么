---
title: Skill、MCP、RAG、Agent 都是什么?
date: 2026-03-27 22:38:04
description: 笔记
tags:
 - AI
---

[toc]

## 1. RAG 

**Retrieval-Augmented Generation（检索增强生成）**

**核心：解决“幻觉”和“私有数据”问题。** LLM 的知识停留在训练结束的那一刻。如果你问它“我公司去年的埋点数据是多少？”，它只能瞎编

- **做法**：先去你的私有数据库或文档里**检索**相关信息，把信息塞给 LLM，让它“看着资料说话”
- **类比**：考试时允许你翻阅相关的《参考资料》

## 2. Skill

**核心：解决“只会说，不会做”的问题。**它是一段具体的代码函数，比如 `查询天气()`、`发送邮件()`、`执行 SQL()`

- **做法**：开发者定义好函数的输入输出和**描述信息**，LLM 根据描述判断什么时候该“调用”这个函数
- **类比**：专家学会了使用 Excel 或发邮件

## 3. Agent (智能体)

**核心：解决“需要人一步步下指令”的问题。** Agent 是最高层级的概念。它利用 LLM 的推理能力，通过 **思考 -> 行动 -> 观察 (Reason -> Act -> Observe)** 的循环来完成复杂目标。

- **做法**：你给它一个模糊目标（如“帮我分析上周埋点报错的原因并写份报告”），它会自己决定先去 RAG 查文档，再用 Skill 调数据库，最后总结

### 核心能力

1. **工具调用**：可以使用各种API、软件、服务
2. **任务规划**：拆解复杂任务为多个步骤
3. **自主决策**：根据结果调整执行策略

## 4. MCP

**Model Context Protocol（模型上下文协议）**

**核心：解决“工具太多，接口不统一”的问题**（为AI工具制定统一标准，就像USB接口统一了设备连接）

### 解决了什么问题？

**在MCP之前**：

- 每个AI工具都有自己的接口标准
- 开发者需要为不同AI分别开发插件
- 工具无法跨平台复用

**有了MCP之后**：

- 统一的工具协议标准
- 一次开发，所有支持MCP的AI都能用
- 工具生态可以快速扩展

## 5. Skill vs MCP工具

| 对比维度 | MCP工具            | Skill                 |
| :------- | :----------------- | :-------------------- |
| 定位     | 底层能力（螺丝刀） | 完整方案（装修服务）  |
| 复杂度   | 单一功能           | 组合多个工具+业务逻辑 |
| 使用场景 | 开发者调用         | 终端用户直接使用      |

**Skill的价值**

1. **降低门槛**：用户无需了解底层工具，直接说"我要发小红书"
2. **最佳实践**：封装领域专家的经验和流程
3. **可组合**：一个Skill可以调用多个MCP工具

## 6. AI 应用架构核心组件深度对比表

| **维度**          | **RAG (检索增强生成)**                            | **MCP (模型上下文协议)**                     | **Skill (智能体技能)**                       | **Agent (智能体/代理)**                         |
| ----------------- | ------------------------------------------------- | -------------------------------------------- | -------------------------------------------- | ----------------------------------------------- |
| **技术本质**      | **知识辅助系统** (Knowledge Augmentation)         | **通用连接协议** (Connectivity Standard)     | **业务逻辑封装** (Encapsulated Logic)        | **自主决策实体** (Autonomous Entity)            |
| **解决核心痛点**  | 解决模型长效记忆缺失与信息幻觉（Hallucination）。 | 解决多源数据、工具接口不统一与重复开发问题。 | 解决原子工具缺乏业务上下文与流程控制的问题。 | 解决复杂任务下的多步拆解、规划与自我修正。      |
| **操作粒度**      | **数据片段级**：基于向量相似度的 Top-K 检索。     | **接口协议级**：标准化的 JSON-RPC 远程调用。 | **任务方案级**：组合多个原子工具的 SOP。     | **目标导向级**：基于推理链 (CoT) 的全流程调度。 |
| **交互模式**      | 检索 -> 注入 Prompt -> 模型回答。                 | 客户端(Client) <-> 服务端(Server) 资源挂载。 | 接收指令 -> 逻辑编排 -> 状态转化 -> 输出。   | 感知(Perception) -> 规划(Planning) -> 执行。    |
| **扩展性/复用性** | 强，只需更新向量数据库内容。                      | 极强，跨模型、跨框架、插拔式接入。           | 中，通常与特定领域或私有业务深度绑定。       | 低，通常根据特定角色（Persona）定制。           |
| **典型技术栈**    | Vector DB (Pinecone), Embedding, LangChain。      | JSON-RPC, MCP SDK (Typescript/Python)。      | Function Calling, Workflow Engines, Hooks。  | LangGraph, AutoGPT, ReAct Prompting。           |